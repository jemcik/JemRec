Nothing in the app changed. 0.3 was checked against a build of the same source on a second machine and two things differed by machine: the native TLS library had been stripped only where an NDK happened to be installed, and the shell-side recorder's bytecode depended on which JDK compiled it. Both are pinned now. This is the first release F-Droid can reproduce and publish with the same signing key as the one here.
