The APK is 6.7 MB instead of 38 MB. Nothing was taken out of the app: the release build now drops the parts of its libraries it never calls, the copies of its TLS library for processors no phone has, and a megabyte of lookup tables for a signature scheme it never uses. Class names are kept, so a crash report still reads as one. Checked on a phone from a fresh install through pairing and a passing self-test.
